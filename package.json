{
  "name": "yjs-datatools",
  "version": "0.0.2",
  "description": "Collections of tools to be source-included, i.e., must install together with dependencies (all here is devDependencies).",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vueuse/core": "^13.9.0",
    "js-yaml": "^4.1.0",
    "vue": "^3.5.21",
    "y-indexeddb": "^9.0.12",
    "y-websocket": "^3.0.0",
    "yjs": "^13.6.27"
  }
}
